# 饭电后端 - 操作说明

## 环境要求

| 组件 | 版本 | 是否已装 |
|------|------|----------|
| JDK | 1.8+ | 自行安装 |
| Maven | 3.6+ | 自行安装 |
| MySQL | 8.4.9 | 已装好 |

## 一、启动 MySQL

每次开发前先启动数据库，双击：

```
饭电-backend\start-mysql.bat
```

看到「MySQL 已启动」即可。窗口可以关掉，MySQL 在后台运行。

停止数据库（比如关机前），双击：

```
饭电-backend\stop-mysql.bat
```

> MySQL 的 root 密码为空。数据文件都存在 `F:\桌面\软工\mysql-data\` 中，不要删这个文件夹。

---

## 二、启动后端项目

在终端中进入项目目录：

```
cd 饭电-backend
```

首次运行先装依赖：

```
mvn clean compile
```

然后启动：

```
mvn spring-boot:run
```

看到 `Started FandianApplication` 就说明启动成功了，端口是 8080。

---

## 三、接口说明

### 1. 微信登录

```
POST http://localhost:8080/api/user/login
Content-Type: application/json

{
    "code": "微信临时凭证",
    "nickname": "用户昵称"
}
```

> code 由前端 `wx.login()` 获取，后端用它去微信服务器换 OpenID。测试时随便传一个字符串即可。

成功返回：
```json
{
    "code": 200,
    "msg": "登录成功",
    "data": {
        "openid": "xxx"
    }
}
```

### 2. 随机抽选菜品

```
GET http://localhost:8080/api/food/random
Authorization: 你的openid
```

成功返回：
```json
{
    "code": 200,
    "data": {
        "name": "黄焖鸡",
        "price": 15.0,
        "canteen": "教一食堂",
        "window": "12号窗口"
    }
}
```

### 3. 数据同步（给爬虫用）

```
POST http://localhost:8080/api/admin/sync
Content-Type: application/json
Sync-Key: your_sync_secret_key

[
    {
        "name": "黄焖鸡",
        "price": 15.0,
        "canteen": "教一食堂",
        "window": "12号"
    },
    {
        "name": "麻辣烫",
        "price": 12.0,
        "canteen": "教一食堂",
        "window": "8号"
    }
]
```

---

## 四、测试接口

用 curl 命令可以在终端里直接测试，无需前端。

### 测试登录

```
curl -X POST http://localhost:8080/api/user/login ^
  -H "Content-Type: application/json" ^
  -d "{\"code\":\"test123\", \"nickname\":\"测试用户\"}"
```

会返回一个 openid，把它记下来。

### 测试随机抽选（假设 openid 是 test_openid_123）

```
curl http://localhost:8080/api/food/random -H "Authorization: test_openid_123"
```

如果数据库还没有菜品，会返回 404「食堂暂无菜品」。

### 先灌几条测试数据

```
curl -X POST http://localhost:8080/api/admin/sync ^
  -H "Content-Type: application/json" ^
  -H "Sync-Key: your_sync_secret_key" ^
  -d "[{\"name\":\"黄焖鸡\",\"price\":15.0,\"canteen\":\"教一食堂\",\"window\":\"12号\"}, {\"name\":\"麻辣烫\",\"price\":12.0,\"canteen\":\"一食堂\",\"window\":\"8号\"}, {\"name\":\"炸鸡饭\",\"price\":13.5,\"canteen\":\"二食堂\",\"window\":\"5号\"}]"
```

然后再调随机抽选就能看到结果了。

---

## 五、查看数据库

如果装了 DBeaver 或 Navicat，连接信息：

| 项 | 值 |
|---|---|
| 地址 | 127.0.0.1 |
| 端口 | 3306 |
| 用户名 | root |
| 密码 | 留空 |
| 数据库 | fandian |

没有 GUI 工具的话，也可以用命令行：

```
"C:\Program Files\MySQL\MySQL Server 8.4\bin\mysql" -u root --host=127.0.0.1 fandian
```

进去后：

```
-- 看所有菜
SELECT * FROM t_food;

-- 看所有用户
SELECT * FROM t_user;
```

---

## 六、配置修改

需要改的配置都在 `src\main\resources\application.yml`：

| 配置项 | 说明 |
|--------|------|
| `wechat.appid` | 微信小程序 AppID（上线前填） |
| `wechat.secret` | 微信小程序 Secret（上线前填） |
| `sync.secret-key` | 同步密钥，Python 爬虫要配一样的 |

---

## 七、常见问题

**Q: 启动报 "Communications link failure"**

A: MySQL 没启动，先双击 `start-mysql.bat`

**Q: 端口 3306 被占用**

A: 之前已经启动过了，不用重复启动，或者关掉占用的进程

**Q: 端口 8080 被占用**

A: 改 `application.yml` 里的 `server.port` 为其他端口，比如 8081

**Q: 换电脑或重装系统了怎么办**

A: 需要重新装 MySQL，把 `F:\桌面\软工\mysql-data\` 备份出来，新机器上装好 MySQL 8.0 后把数据文件夹放回去
